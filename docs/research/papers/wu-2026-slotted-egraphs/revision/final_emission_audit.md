# Independent final emission audit

**Scientific sources and reviewed build: PASS. No scientific-source repair or material blocker remains.**

This decision applies to the exact 25-file LaTeX/bibliography/template/graphic closure recorded in `final_emission_audit.json` and PDF SHA-256 `3165c447bfb2d365d0c0ecb273e90dcbe98f6a5df958f0df773c444393133a0f`. Parent is refreshing README and package metadata; the final packaging must bind these reviewed bytes. The auditor did not modify the manuscript, repository, or formal package.

## Requested readability and preservation

The revised Background explains an e-node, an e-class, congruence, hash-consing, rebuilding, equality saturation and extraction around the unchanged arithmetic example. It explains ACI by the separate treatment of association, order and repetition; introduces alpha-equivalence and open-term dependence; explains caller slot maps and type preservation; and introduces Alloy signatures, relations, multiplicities, predicates, joins, assertions, object scopes, trace bounds and student/oracle labels. These explanations preserve the paper's conditional equality and semantic boundaries. They provide the context requested by the user without adding a new formal result.

The original introduction, conclusion, main formal section, proofs, worked Alloy figure, algorithm, bibliography, class and bibliography style remain byte-identical. Seven TeX files changed. Their hunks are confined to the explicitly authorized Background work (U), verified empirical synchronization and necessary dependent coherence (E). Moving Alloy basics into Background is paired with a correct evaluation cross-reference. The arithmetic and Alloy examples' equations are unchanged. The inspector retains 21 TeX files, one bibliography and one graphic, 86 labels and 21 citation keys; the reference-key count grows from 34 to 35 because evaluation now refers to Background. No missing asset, duplicate label, undefined reference/citation, or unresolved dynamic input is reported.

## Evidence fidelity

Every changed performance cell was independently recalculated from `egraph_ablation/comparison.json`: all 28 wall/CPU/RSS/representation cells match the seven corresponding records. All 77 capability cells and seven totals match `capability_benchmark/results.json`. The unchanged distance, size, truth-consolidation and radius-coverage values retain their denominators and view-specific units. Ratios are correctly rounded to 111.885 natural process wall, 593.026 engine CPU and 86.920 targeted process wall. Peak CI heap is 7,137.811 MiB and maximum RSS is 8,943.988 MiB. The ratio prose now states the multiplication unambiguously.

The manuscript distinguishes frozen retrieval commit `1e2667351532b0c632166fa21ae5fbc7308a8fe7` from result-producing source `8ad5fead39b687d2cadc79b01ac27743c1ece990` and full publication run `db9f89bf-0965-4d74-8080-d9191d5f1aec`. Its original JAR and natural dataset identities match the manifest. Later validation run `659e248c-d3d6-4a2b-8d99-67a0ebcf9eb4`, source `8feab00f9190482af6a25334af5b2716653f8ac9`, has a separate JAR, one worker and 1 GiB heap. All 29 sampled checks have no counterexample, error or inconclusive result. Each of the eight temporal checks records scope 4 and trace bounds 1–10; six expose temporal mode after the checker repair. The source does not conflate 29 sampled solver checks, 5,500 transformation-recognition pairs or 4,088 natural-corpus claim identities.

The oracle-plus-CORRECT-student pool is now accurate, including raw-AST deduplication and within-invariant comparison. Existing chart bytes remain supported because the old and current source SVG have the same Git blob identity. The matrix binder-owner mapping sentence is supported by `metric/QuotientRepairDistance.java` lines 566–591 and 780–809 and the related reference implementation in `core/CanonicalDistance.java` lines 414–422. All 11 currently supplied claim-ledger evidence hashes match local pinned evidence.

## Notation, logic and archived formal evidence

The 41 grouped baseline notation rows remain consistent. `final_notation_ledger.json` records six explanatory/evidence deltas and zero unresolved material rows. Typed invocation construction, embedding action, invertible renaming, proper inclusion, descriptor automorphism, graph symmetry, structural quotient, endpoint certificate, model equality and implementation distance zero remain distinct. No formal type, arity, binder, quantifier, equality notion or theorem premise changed.

Independently parsed `formal/formal-units.yaml` identifies 146 active source spans. All 146 match both original and revised source bytes and all 146 registered excerpt hashes. All 75 files under `formal/` are unchanged, including Lean source/configuration, generated index and archived evidence. This verifies preservation; it is not a new Lean execution or a new review of the whole manuscript. Historical full-source snapshot hashes inside these preserved reports do not identify the expanded Background/current paper. New packaging must say this explicitly.

The artifact-refinement table's new archived snapshot `6764808` matches full JSON identity `676480899dc2ed29253d00c59beaf116ebd2c675`. Its unchanged component-level rows are explicitly separated from newer bounded constructor/record replays. No old archive row has been promoted, and the new text does not turn bounded source/observation results into full Java/parser refinement or corpus replay.

## Build and layout

The reviewed pdfLaTeX/BibTeX output has 49 pages, with main text ending on page 20 and references beginning on page 21. It meets the recorded 20-page main-body limit. There are zero Overfull messages and no undefined references or citations. The two inherited font-substitution warnings remain; Underfull diagnostics increase from 363 to 364, dominated by existing narrow formal-index/appendix tables. No missing glyph was reported.

I visually inspected pages 2–4, 15–20, 32–34 and 37–39, including expanded Background, the existing Alloy example, metric equation, plot/headline table, cost and validation claims, dated archive table, full provenance, seven-arm tables and appendix transition. No clipping, overlap, broken equation, missing label, or other material introduced layout defect was found. Inherited table density and page whitespace do not justify unrelated layout edits. Parent performs the full-document page review.

## Limits and final packaging condition

No experiments were rerun. The 253 MB LFS raw distance file and large index were not independently downloaded or reaggregated; the checks rely on exact manifest-bound generated summaries and selected structured aggregates. No new Lean build, axiom audit or universal Java/parser correspondence is claimed. The bundled LNCS class/style were retained; this is not a new official venue-template certification.

The exact scientific diff was regenerated as `final_auditor_scientific.diff`, including the final “no new nodes or equalities” wording. Refresh the earlier diff/change-manifest hashes, README, package inventory and current build/source identities before packaging. Those metadata updates need not reopen scientific review provided the recorded scientific-source and PDF hashes remain unchanged.
